import SwiftUI

/// Placeholder for future orbital overlays. Currently empty — icons removed.
struct OrbitalIconOverlay: View {
    var body: some View {
        Color.clear
            .allowsHitTesting(false)
    }
}
